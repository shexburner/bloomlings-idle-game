// =============================================================================
// Collection Tab — Bloomling collection grid with detail view
// =============================================================================

import { CollectionGrid } from "~/components/collection/CollectionGrid";

export default function CollectionScreen() {
  return <CollectionGrid />;
}
